r1 = float(input('Digite o valor do primeiro lado do triângulo:'))
r2 = float(input('Digite o valor do segundo lado do triângulo:'))
r3 = float(input('Digite o valor do terceiro lado do triângulo:'))
if r1 < r2 + r3 and r2 < r1 + r3 and r3 < r1 + r2:
    print('Os segmentos podem formar um triângulo!')
else:
    print('Os segmentos não podem formar um triângulo!')
